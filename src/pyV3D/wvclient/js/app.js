'use strict';


// Declare app level module which depends on filters, and services
var webviewerApp = angular.module('WebViewerApp', ['WebViewerApp.filters', 'WebViewerApp.services', 'WebViewerApp.directives'])

//WVapp.config(['$routeProvider', function($routeProvider) {
//    $routeProvider.when('/', {templateUrl: 'partials/wv.html', controller: wvCtrl});
//  }]);

